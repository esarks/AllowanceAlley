# Wiring Patch v2

Pick ONE option:

- **Option A:** replace `AllowanceAlleyApp.swift` to make `ContentViewBRD()` your root and inject required stores.
- **Option B:** keep your current root; replace your `Chores*` and `Rewards*` views with forwarders so your existing tabs render the working screens.

After applying: Clean Build Folder → Run.

// ProfileView.swift
import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Profile").font(.title2)
            Text("Coming soon")
        }
        .padding()
    }
}
